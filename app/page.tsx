const Home = () => {
  return <div></div>;
};
// description page if not logged in, home page if logged in with the ability to search through categories and pick whichever u wanna see <3 i love daria
export default Home;
